import { appConstants } from '@constant/appConstants';

export const resourcePath = {
  province: `${appConstants.hostPath}/api/provinces`,
  district: `${appConstants.hostPath}/api/districts`,
  ward: `${appConstants.hostPath}/api/wards`,
  post: `${appConstants.hostPath}/api/posts`,
  postCategory: `${appConstants.hostPath}/api/post-categories`,
  postComment: `${appConstants.hostPath}/api/post-comments`,
};
